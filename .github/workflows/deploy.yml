name: CD

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy locally on VPS
        run: |
          cd /home/wigamu/wgsoft
          git pull
          cd /home/wigamu/wgsoft/infra
          docker-compose up -d --build
